spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    # URL, username, password будут переопределены из Testcontainers через @DynamicPropertySource
  liquibase:
    enabled: true
    change-log: classpath:/db/migration/db.changelog-master.yaml



